<ul>
  <% @comments.each do |comment| %>
    <% if comment.id.present? %>
      <li>
        <p><br><%= comment.description %><br>
        <% if logged_in? %>
        <%= link_to "delete", [comment.book, comment], method: :delete, class:"btn btn-danger" %>
        <%= link_to "edit ", edit_book_comment_path(comment.book, comment), class:"btn btn-primary" %></p>
        </li>
        <% end %>
    <% end %>
  <% end %>
<%= render 'comments/form'%>
</ul>